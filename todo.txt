TODO

- Group all the admin pages in an "admin" subfolder.
- Group all user pages in a "user" subfolder.
- pages for user.
- custom error message for inactive form.
- login form HTML generation. Two options for unsuccessful login attempts:
    1. (default) redirect to the main login page
    2. redirect to another page with GET string.

Required fields:
  form_tools_form_id
  username
  password


Optional fields:
  invalid_login_redirect_url

If redirected to another page, the following info is sent via GET:
  - "error_code" Values:
  		  no_form_id
  		  no_username
  		  no_password
  		  invalid_form_id
  		  login_incorrect
  		  form_inactive
  - "username" (so you can re-fill the username field, if you like)
  - "form_tools_form_id" (if available)


Documentation:
- Only supports a single language right now.
